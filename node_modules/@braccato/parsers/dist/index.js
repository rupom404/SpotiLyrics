import { XMLParser as tt } from "fast-xml-parser";
const et = 5e3;
function L(t, e, s = et) {
  if (t.length === 0) return t;
  const n = [], r = (o, c) => ({
    startTimeMs: o,
    durationMs: c,
    words: "",
    parts: [],
    isInstrumental: !0
  });
  t[0].startTimeMs > s && n.push(r(0, t[0].startTimeMs));
  for (let o = 0; o < t.length; o++)
    if (n.push(t[o]), o < t.length - 1) {
      const c = t[o].startTimeMs + t[o].durationMs, d = t[o + 1].startTimeMs - c;
      d > s && n.push(r(c, d));
    }
  const i = t[t.length - 1], l = i.startTimeMs + i.durationMs, a = e - l;
  return a > s && n.push(r(l, a)), n;
}
function v(t) {
  if (!t) return 0;
  if (typeof t == "number") return t;
  const e = t.match(/^([\d.]+)(h|m|s|ms)$/);
  if (e) {
    const r = Number.parseFloat(e[1]), i = e[2];
    if (i === "h") return Math.round(r * 60 * 60 * 1e3);
    if (i === "m") return Math.round(r * 60 * 1e3);
    if (i === "s") return Math.round(r * 1e3);
    if (i === "ms") return Math.round(r);
  }
  const s = t.split(":").map((r) => r.replace(/[^0-9.]/g, ""));
  let n = 0;
  try {
    return s.length === 1 ? n = Number.parseFloat(s[0]) * 1e3 : s.length === 2 ? n = Number.parseInt(s[0], 10) * 60 * 1e3 + Number.parseFloat(s[1]) * 1e3 : s.length === 3 && (n = Number.parseInt(s[0], 10) * 3600 * 1e3 + Number.parseInt(s[1], 10) * 60 * 1e3 + Number.parseFloat(s[2]) * 1e3), Math.round(n);
  } catch {
    return 0;
  }
}
function nt(t) {
  var r, i;
  const e = /* @__PURE__ */ new Map();
  if (!t || t.length === 0) return e;
  const s = t.filter((l) => "agent" in l && l[":@"]);
  let n = 0;
  for (const l of s) {
    const a = (r = l[":@"]) == null ? void 0 : r["@_id"], o = (i = l[":@"]) == null ? void 0 : i["@_type"];
    a && (o === "person" || o === "character" ? (n++, e.set(a, `v${n}`)) : e.set(a, "v1000"));
  }
  return e;
}
function O(t) {
  if (!t) return "";
  let e = "";
  for (const s of t)
    typeof s["#text"] == "string" ? e += s["#text"] : s.span && (e += O(s.span));
  return e;
}
function Z(t, e) {
  var i, l, a, o, c;
  let s = "", n = [], r = !1;
  for (const u of t) {
    let d = !1, g = [u];
    ((i = u[":@"]) == null ? void 0 : i["@_role"]) === "x-bg" && (d = !0, g = u.span ?? []);
    for (const f of g)
      if (f["#text"]) {
        s += f["#text"];
        const m = n[n.length - 1];
        n.push({
          startTimeMs: m ? m.startTimeMs + m.durationMs : e,
          durationMs: 0,
          words: f["#text"],
          isBackground: d
        });
      } else if (f.span) {
        const m = O(f.span);
        if (!m) continue;
        const T = v((l = f[":@"]) == null ? void 0 : l["@_begin"]), h = v((a = f[":@"]) == null ? void 0 : a["@_end"]), _ = ((o = f[":@"]) == null ? void 0 : o["@_explicit"]) === "true" || ((c = f[":@"]) == null ? void 0 : c["@_obscene"]) === "true";
        n.push({
          startTimeMs: T,
          durationMs: h - T,
          isBackground: d,
          words: m,
          explicit: _
        }), s += m, r = !0;
      }
  }
  return r || (n = []), { parts: n, text: s, isWordSynced: r };
}
const st = /<\/?([A-Za-z][\w.-]*):/g, rt = /\s([A-Za-z][\w.-]*):[\w.-]+\s*=/g, ot = /xmlns:([A-Za-z][\w.-]*)\s*=/g, at = /<tt\b[^>]*>/;
function it(t) {
  const e = t.match(at);
  if (!e) return t;
  const s = e[0], n = /* @__PURE__ */ new Set(["xml", "xmlns"]);
  for (const o of s.matchAll(ot))
    n.add(o[1]);
  const r = /* @__PURE__ */ new Set();
  for (const o of t.matchAll(st))
    r.add(o[1]);
  for (const o of t.matchAll(rt))
    r.add(o[1]);
  const i = [...r].filter((o) => !n.has(o));
  if (i.length === 0) return t;
  const l = i.map((o) => ` xmlns:${o}="urn:braccato:unbound:${o}"`).join(""), a = s.replace(/>$/, `${l}>`);
  return t.replace(s, a);
}
const lt = {
  ignoreAttributes: !1,
  attributeNamePrefix: "@_",
  attributesGroupName: !1,
  textNodeName: "#text",
  trimValues: !1,
  removeNSPrefix: !0,
  preserveOrder: !0,
  allowBooleanAttributes: !0,
  parseAttributeValue: !1,
  parseTagValue: !1
};
function k(t, e) {
  const s = t.find((n) => e in n);
  if (s != null && s[e]) return s;
  for (const n of t)
    for (const r of Object.values(n))
      if (Array.isArray(r)) {
        const i = r.find((l) => typeof l == "object" && l !== null && e in l);
        if (i != null && i[e]) return i;
      }
  return null;
}
function ct(t, e = {}) {
  var $, F, W, X, D, z, j, V;
  const s = new tt(lt), n = t.replace(/\\"/g, '"');
  let r;
  try {
    r = s.parse(it(n));
  } catch {
    return { lyrics: [], isWordSynced: !1 };
  }
  const i = Array.isArray(r) ? r.find((p) => "tt" in p) : void 0, l = i == null ? void 0 : i.tt;
  if (!l) return { lyrics: [], isWordSynced: !1 };
  const a = ($ = l.find((p) => p.head)) == null ? void 0 : $.head, o = l.find((p) => p.body), c = o == null ? void 0 : o.body, u = o == null ? void 0 : o[":@"], d = ((F = i == null ? void 0 : i[":@"]) == null ? void 0 : F["@_lang"]) || (u == null ? void 0 : u["@_lang"]);
  if (!c) return { lyrics: [], isWordSynced: !1, language: d };
  const g = /* @__PURE__ */ new Map(), f = {}, m = ((W = a == null ? void 0 : a.find((p) => "metadata" in p)) == null ? void 0 : W.metadata) ?? [], T = nt(m), h = c.flatMap((p) => p.div ?? []).filter((p) => p != null && "p" in p);
  if (!(h.length > 0 && h[0][":@"] !== void 0))
    return { lyrics: [], isWordSynced: !1, language: d };
  let S = !1;
  for (const p of h) {
    const b = p[":@"];
    if (!(b != null && b["@_begin"])) continue;
    const y = v(b["@_begin"]), R = v(b["@_end"]), E = Z(p.p, y);
    E.isWordSynced && (S = !0);
    const A = b["@_agent"], P = A ? T.get(A) ?? A : void 0, I = b["@_key"];
    let w;
    if (I) {
      const G = f[I];
      G ? G.push(`${I}_${G.length + 1}`) : f[I] = [`${I}_1`];
      const K = f[I];
      w = K[K.length - 1];
    } else
      w = String(g.size);
    g.set(w, {
      agent: P,
      durationMs: R - y,
      parts: E.parts,
      startTimeMs: y,
      words: E.text,
      key: I ?? w
    });
  }
  const x = (p, b) => {
    const y = f[p];
    if (y)
      for (const R of y) {
        const E = g.get(R);
        E && b(E);
      }
  }, M = k(m, "translations");
  if (M) {
    const p = (X = M[":@"]) == null ? void 0 : X["@_lang"], b = M.translations ?? [];
    for (const y of b) {
      const R = ((D = y[":@"]) == null ? void 0 : D["@_lang"]) ?? p, E = (z = y[":@"]) == null ? void 0 : z["@_for"];
      for (const A of y.translation ?? []) {
        const P = ((j = A[":@"]) == null ? void 0 : j["@_for"]) ?? E, I = O(A.text);
        !R || !I || !P || x(P, (w) => {
          w.translations || (w.translations = {}), w.translations[R] = I, w.translation || (w.translation = { text: I, lang: R });
        });
      }
    }
  }
  const N = k(m, "transliterations");
  if (N) {
    const p = N.transliterations ?? [];
    for (const b of p)
      for (const y of b.transliteration ?? []) {
        const R = (V = y[":@"]) == null ? void 0 : V["@_for"], E = y.text;
        !R || !E || x(R, (A) => {
          const P = Z(E, A.startTimeMs);
          A.romanization = P.text, P.parts.length > 0 && (A.timedRomanization = P.parts);
        });
      }
  }
  const C = u != null && u["@_dur"] ? v(u["@_dur"]) : e.songDurationMs ?? 0;
  return { lyrics: L(Array.from(g.values()), C, e.instrumentalGapMs), isWordSynced: S, language: d };
}
const ut = {
  detect(t) {
    return t.includes("<tt") && t.includes("</tt>");
  },
  // The duration is deliberately unused. `@braccato/provider-blyrics` calls this with one argument,
  // and a version that honoured the parameter would hand that call site a 0 song duration.
  parse(t, e = 0) {
    return ct(t).lyrics;
  }
}, ft = ["ti", "ar", "al", "au", "lr", "length", "by", "offset", "re", "tool", "ve", "#"], B = /\[(\d+:\d+\.\d+)\]/g, dt = /<(\d+:\d+\.\d+)>/g, mt = /^\[(\w+):(.*)\]$/;
function U(t) {
  if (!t) return 0;
  if (typeof t == "number") return t;
  const e = t.split(":");
  let s = 0;
  try {
    if (e.length === 1)
      s = Number.parseFloat(e[0]) * 1e3;
    else if (e.length === 2) {
      const n = Number.parseInt(e[0], 10), r = Number.parseFloat(e[1]);
      s = n * 60 * 1e3 + r * 1e3;
    } else if (e.length === 3) {
      const n = Number.parseInt(e[0], 10), r = Number.parseInt(e[1], 10), i = Number.parseFloat(e[2]);
      s = n * 3600 * 1e3 + r * 60 * 1e3 + i * 1e3;
    }
    return Math.round(s);
  } catch {
    return 0;
  }
}
function pt(t, e) {
  var i;
  const s = t.split(`
`), n = [], r = {};
  for (const l of s) {
    const a = l.trim(), o = a.match(mt);
    if (o && ft.includes(o[1])) {
      r[o[1]] = o[2];
      continue;
    }
    let c = Number.POSITIVE_INFINITY, u = Number.NEGATIVE_INFINITY, d = 0;
    for (const x of a.matchAll(B)) {
      const M = U(x[1]);
      M < c && (c = M), M > u && (u = M), d++;
    }
    if (d === 0) continue;
    const g = a.replace(B, "").trim(), f = [];
    let m = null, T = "";
    const h = g.split(dt), _ = h.some(
      (x, M) => M % 2 === 0 && M > 0 && M < h.length - 1 && x.length > 0 && x.trim() === ""
    );
    h.forEach((x, M) => {
      if (M % 2 === 0) {
        let N = x;
        if (_) {
          const C = N.trim();
          N = C === "" && N.length > 0 ? " " : C;
        }
        T += N, f.length > 0 && (f[f.length - 1].words += N);
      } else {
        const N = U(x);
        m !== null && f.length > 0 && (f[f.length - 1].durationMs = N - m), f.push({ startTimeMs: N, words: "", durationMs: 0 }), m = N;
      }
    });
    const S = u - c;
    n.push({
      startTimeMs: c,
      words: T.trim(),
      durationMs: S,
      parts: f.length > 0 ? f : void 0
    });
  }
  for (let l = 0; l < n.length; l++) {
    const a = n[l];
    if (l + 1 < n.length) {
      const o = n[l + 1];
      if (a.durationMs === 0 && (a.durationMs = Math.max(o.startTimeMs - a.startTimeMs, 0)), a.parts && a.parts.length > 0) {
        let c = o.startTimeMs;
        for (const d of a.parts)
          c = Math.max(c, d.startTimeMs);
        const u = a.parts[a.parts.length - 1];
        u.durationMs = Math.max(o.startTimeMs - u.startTimeMs, 0), a.durationMs = Math.max(c - a.startTimeMs, 0);
      }
    } else {
      const o = e - a.startTimeMs;
      if (a.durationMs === 0 && o > 0 && (a.durationMs = o), a.parts && a.parts.length > 0) {
        const c = a.parts[a.parts.length - 1], u = e - c.startTimeMs;
        u > 0 && (c.durationMs = u);
      }
    }
  }
  if (r.offset) {
    let l = Number(r.offset);
    Number.isNaN(l) && (l = 0), l = l * 1e3;
    for (const a of n)
      a.startTimeMs -= l, (i = a.parts) == null || i.forEach((o) => {
        o.startTimeMs -= l;
      });
  }
  return n;
}
function gt(t) {
  for (const n of t)
    if (n.parts)
      for (let r = 1; r < n.parts.length; r++) {
        const i = n.parts[r], l = n.parts[r - 1];
        if (i.words === " " && l.words !== " ") {
          const a = i.durationMs - l.durationMs;
          if (Math.abs(a) <= 15 || i.durationMs <= 100) {
            const o = i.durationMs;
            l.durationMs += o, i.durationMs -= o, i.startTimeMs += o;
          }
        }
      }
  let e = 0, s = 0;
  for (const n of t)
    if (!(!n.parts || n.parts.length === 0))
      for (let r = 0; r < n.parts.length - 2; r++) {
        const i = n.parts[r];
        i.words !== " " && (i.durationMs <= 100 && e++, s++);
      }
  if (s > 0 && e / s > 0.5)
    for (let n = 0; n < t.length; n++) {
      const r = t[n];
      if (!(!r.parts || r.parts.length === 0))
        for (let i = 0; i < r.parts.length; i++) {
          const l = r.parts[i];
          if (l.words !== " " && l.durationMs <= 400) {
            let a = null;
            i + 1 < r.parts.length ? a = r.parts[i + 1] : n + 1 < t.length && t[n + 1].parts && t[n + 1].parts.length > 0 && (a = t[n + 1].parts[0]), a === null ? l.durationMs = 300 : a.words === " " ? (l.durationMs += a.durationMs, a.startTimeMs += a.durationMs, a.durationMs = 0) : l.durationMs = a.startTimeMs - l.startTimeMs;
          }
        }
    }
}
const ht = {
  detect(t) {
    return /\[\d+:\d+\.\d+\]/.test(t);
  },
  parse(t, e = 0) {
    const s = pt(t, e);
    return gt(s), s;
  }
};
function Y(t) {
  const e = t.trim().match(/^(\d+):(\d+):(\d+)[,.](\d+)$/);
  if (!e) return 0;
  const s = Number.parseInt(e[1], 10), n = Number.parseInt(e[2], 10), r = Number.parseInt(e[3], 10), i = Number.parseInt(e[4].padEnd(3, "0").slice(0, 3), 10);
  return s * 36e5 + n * 6e4 + r * 1e3 + i;
}
const Mt = {
  detect(t) {
    return /\d+\r?\n\d{2}:\d{2}:\d{2}[,.]\d+ --> \d{2}:\d{2}:\d{2}[,.]\d+/.test(t);
  },
  parse(t, e = 0) {
    const s = t.trim().split(/\r?\n\r?\n/), n = [];
    for (const r of s) {
      const i = r.split(/\r?\n/);
      if (i.length < 2) continue;
      const l = i.find((f) => f.includes("-->"));
      if (!l) continue;
      const [a, o] = l.split("-->").map((f) => f.trim()), c = Y(a), u = Y(o), d = i.indexOf(l), g = i.slice(d + 1).join(`
`).replace(/<[^>]+>/g, "").trim();
      g && n.push({
        startTimeMs: c,
        words: g,
        durationMs: u - c
      });
    }
    return n;
  }
}, q = {
  detect(t) {
    return !0;
  },
  parse(t, e = 0) {
    return t.split(`
`).map((s) => ({
      startTimeMs: 0,
      words: s,
      durationMs: 0
    }));
  }
};
/**
 * @author Stephen Brown
 * Source: https://github.com/stephenjjbrown/string-similarity-js/
 * @license MIT License - https://github.com/stephenjjbrown/string-similarity-js/blob/master/LICENSE.md
 * @param str1 First string to match
 * @param str2 Second string to match
 * @param [substringLength=2] Optional. Length of substring to be used in calculating similarity. Default 2.
 * @param [caseSensitive=false] Optional. Whether you want to consider case in string matching. Default false;
 * @returns Number between 0 and 1, with 0 being a low match score.
 */
const Tt = (t, e, s = 2, n = !1) => {
  const r = n ? t : t.toLowerCase(), i = n ? e : e.toLowerCase();
  if (r.length < s || i.length < s) return 0;
  const l = /* @__PURE__ */ new Map();
  for (let o = 0; o < r.length - (s - 1); o++) {
    const c = r.substring(o, o + s);
    l.set(c, l.has(c) ? l.get(c) + 1 : 1);
  }
  let a = 0;
  for (let o = 0; o < i.length - (s - 1); o++) {
    const c = i.substring(o, o + s), u = l.has(c) ? l.get(c) : 0;
    u > 0 && (l.set(c, u - 1), a++);
  }
  return a * 2 / (r.length + i.length - (s - 1) * 2);
};
function bt(t) {
  if (t[0] !== "[") return null;
  const e = t.indexOf("]");
  if (e === -1) return null;
  const s = t.indexOf(",", 1);
  if (s === -1 || s > e) return null;
  const n = Number.parseInt(t.slice(1, s), 10), r = Number.parseInt(t.slice(s + 1, e), 10);
  return Number.isNaN(n) || Number.isNaN(r) ? null : { startTime: n, duration: r, rest: t.slice(e + 1) };
}
const yt = /\((\d+),(\d+)\)/g;
function _t(t) {
  const e = [];
  let s = 0;
  for (const n of t.matchAll(yt))
    e.push({
      text: t.slice(s, n.index),
      time: Number.parseInt(n[1], 10),
      duration: Number.parseInt(n[2], 10)
    }), s = n.index + n[0].length;
  return e;
}
const xt = [
  "词",
  "作词",
  "曲",
  "作曲",
  "编曲",
  "和声",
  "混音",
  "吉他",
  "制作人",
  "演唱",
  "原唱",
  "翻唱",
  "后期",
  "和音",
  "录音",
  "策划",
  "伴奏",
  "美工",
  "海报",
  "旁白",
  "writtenby",
  "producedby",
  "composedby",
  "arrangedby",
  "mixing",
  "mastering",
  "vocal",
  "vocals",
  "guitar",
  "bass",
  "drums",
  "producer",
  "lyricist",
  "composer",
  "arranger",
  "lyricsby"
];
function Q(t) {
  const e = t.toLowerCase().replace(/\s+/g, "");
  return xt.includes(e) || e.endsWith("词") || e.endsWith("曲") || e.endsWith("声") || e.endsWith("音");
}
function H(t, e, s) {
  if (!s.aliases[t]) {
    const n = t.toUpperCase(), r = n === "合" || n === "ALL" || n === "合唱" ? "group" : "person";
    s.aliases[t] = r === "group" ? "v1000" : `v${s.nextVoiceId++}`;
  }
  e.agent = s.aliases[t];
}
function J(t) {
  if (t.syllables.length > 0) {
    const e = t.syllables[0], s = t.syllables[t.syllables.length - 1];
    t.time = e.time, t.duration = s.time + s.duration - e.time;
  }
}
const Nt = /[^a-z0-9　-鿿가-힯]/g;
function Et(t, e, s, n) {
  if (!t.syllables.length)
    return e.currentSinger && (t.agent = e.currentSinger), !0;
  if (s) {
    if (n && (n.title || n.artist)) {
      const T = (M) => M.toLowerCase().replace(Nt, ""), h = T(t.text), _ = T(n.title ?? ""), S = T(n.artist ?? "");
      if (_ && h.includes(_) && S && h.includes(S) || _ && h.includes(_) && !S && h.length < _.length + 15 || S && h === S) return !1;
      const x = `${n.title ?? ""} ${n.artist ?? ""}`;
      if (Tt(t.text, x) > 0.5) return !1;
    }
    if (t.syllables.length > 2 && t.syllables.every((T, h, _) => Math.abs(T.duration - _[0].duration) < 10)) return !1;
  }
  e.currentSinger && (t.agent = e.currentSinger);
  let r = "", i = 0;
  for (const m of t.syllables) {
    if (r += m.text, i++, r.includes(":") || r.includes("：")) break;
    if (r.length > 40) return !0;
  }
  const l = r.match(/^([^:：]+)\s*[:：]\s*$/);
  if (l) {
    const m = l[1].trim();
    return Q(m) ? !1 : (m.length > 30 || (t.syllables = t.syllables.slice(i), t.text = t.text.substring(r.length), H(m, t, e), e.currentSinger = e.aliases[m], J(t)), !0);
  }
  const a = r.match(/^([^:：]+)\s*[:：]\s*([\s\S]*)$/);
  if (!a) return !0;
  const o = a[1].trim();
  if (Q(o)) return !1;
  if (o.length > 20) return !0;
  const c = a[2] || "", u = i - 1, d = t.syllables[u], g = d.text.match(/[:：]\s*([\s\S]*)$/), f = g ? g[1] : "";
  return f.length > 0 ? (d.text = f, t.syllables = t.syllables.slice(u)) : t.syllables = t.syllables.slice(i), H(o, t, e), e.currentSinger = e.aliases[o], t.text = c, J(t), !0;
}
const It = /LyricContent="([\s\S]*?)"\s*(?:\/?>|[a-zA-Z]+=)/;
function St(t) {
  const e = t.match(It);
  return e ? e[1].replace(/&quot;/g, '"').replace(/&amp;/g, "&") : t;
}
function Rt(t, e, s) {
  if (!t) return [];
  const n = St(t), r = [], i = { aliases: {}, nextVoiceId: 1, currentSinger: null };
  for (const a of n.split(`
`)) {
    const o = a.trim();
    if (!o || /^\[[a-zA-Z]+:/.test(o)) continue;
    const c = bt(o);
    if (!c) continue;
    const u = _t(c.rest), d = u.map((f) => f.text).join(""), g = {
      time: c.startTime,
      duration: c.duration,
      text: d,
      syllables: u,
      agent: null
    };
    Et(g, i, r.length < 5, s) && r.push(g);
  }
  if (r.length === 0) return [];
  const l = r.map((a, o) => {
    const c = a.syllables.map((d) => ({
      startTimeMs: d.time,
      words: d.text,
      durationMs: d.duration
    }));
    let u = a.duration;
    if (u === 0 && o + 1 < r.length ? u = Math.max(r[o + 1].time - a.time, 0) : u === 0 && (u = e - a.time), c.length > 0) {
      const d = c[c.length - 1];
      d.durationMs === 0 && (o + 1 < r.length ? d.durationMs = Math.max(r[o + 1].time - d.startTimeMs, 0) : d.durationMs = e - d.startTimeMs);
    }
    return {
      startTimeMs: a.time,
      words: a.text,
      durationMs: u,
      parts: c.length > 0 ? c : void 0,
      agent: a.agent ?? void 0
    };
  });
  return L(l, e);
}
const At = {
  detect(t) {
    return t.includes("<QrcInfos>") || t.includes("LyricContent=") ? !0 : /\[\d+,\d+\]/.test(t) && /\(\d+,\d+\)/.test(t);
  },
  parse(t, e = 0) {
    return Rt(t, e);
  }
}, wt = [ut, ht, Mt, At, q];
function Ct(t) {
  for (const e of wt)
    if (e.detect(t)) return e;
  return q;
}
export {
  ht as LRCParser,
  q as PlainParser,
  At as QRCParser,
  Mt as SRTParser,
  ut as TTMLParser,
  Ct as detectParser,
  gt as lrcFixers,
  pt as parseLRC,
  Rt as parseQRC,
  ct as parseTTMLContent,
  v as parseTTMLTime
};
